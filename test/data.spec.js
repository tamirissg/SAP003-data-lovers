require("../src/data.js");

describe("e uma funcao", () => {
  it("is a function", () => {
    expect(typeof app.filterData).toBe("function");
  });

  // it("returns `example`", () => {
  //   expect(example()).toBe("example");
  // });
});

